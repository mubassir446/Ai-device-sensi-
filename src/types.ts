export interface SensitivitySettings {
  general: number;
  redDot: number;
  scope2x: number;
  scope4x: number;
  sniperScope: number;
  freeLook: number;
  fireButtonSize: number;
  dpi: number;
  deviceInfo: string;
}

export type DeviceOS = 'Android' | 'iOS';
